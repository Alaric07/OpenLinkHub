"use strict";
(function(root,factory){const api=factory();if(typeof module==="object"&&module.exports){module.exports=api;}if(root&&root.document){const init=function(){api.init(root);};if(root.document.readyState==="loading"){root.document.addEventListener("DOMContentLoaded",init);}else{init();}}})(typeof window==="undefined"?null:window,function(){function init(browser){const w=browser.document.querySelector("[data-lf-mouse-gestures-workspace]");if(!w){return;}const save=w.querySelector("[data-lf-mouse-gestures-save]"),enabled=w.querySelector("[data-lf-mouse-gestures-enabled]"),status=w.querySelector("[data-lf-mouse-gestures-status]");if(!save||!enabled){return;}save.addEventListener("click",function(){const zoneTilts={};let valid=true;w.querySelectorAll("[data-lf-mouse-gesture-tilt]").forEach(function(row){const id=row.dataset.lfGestureId,value=Number(row.querySelector("[data-lf-mouse-gesture-value]").value);if(!/^\d+$/.test(id)||!Number.isInteger(value)||value<10||value>80){valid=false;return;}zoneTilts[id]=value;});if(!valid||Object.keys(zoneTilts).length!==4){if(status){status.textContent="Enter tilt thresholds from 10 to 80.";}return Promise.resolve();}return browser.fetch("/api/mouse/gestures",{method:"POST",body:JSON.stringify({deviceId:w.dataset.lfDeviceId,multiGestures:enabled.checked?1:0,zoneTilts:zoneTilts})}).then(async function(response){const result=response.ok?await response.json():null;if(!result||result.status!==1){throw new Error("save rejected");}if(browser.LumenForgeDevicesToast){browser.LumenForgeDevicesToast("✓ Saved","success",1500);}}).catch(function(){if(status){status.textContent="Couldn’t save Multi Gesture.";}});});}return{init:init};});
